export default function Footer() {
    return (
        <footer className="cv-footer">
            <span>© {new Date().getFullYear()} UrbnConnect — Report,Act,Sustain</span>
            <span className="cv-text-xs">Built for Hackathon MVP</span>
        </footer>
    );
}
